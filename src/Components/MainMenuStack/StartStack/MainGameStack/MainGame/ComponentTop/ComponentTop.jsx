import React from 'react';
import { ComponentTop as SharedComponentTop } from '../../shared';

const ComponentTop = (props) => <SharedComponentTop mode="game" {...props} />;

export default ComponentTop;